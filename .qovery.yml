application:
  name: makeupapp
  project: makeupapp
  cloud_region: aws/eu-west-3
  publicly_accessible: true
databases:
- type: postgresql
  version: "12.5"
  name: MakeupApp
routers:
- name: main
  routes:
  - application_name: makeupapp
    paths:
    - /*
